<%= provide :title, t(".title") %>
<%= provide :add_button_text, t(".add_category") %>
<%= provide :add_link, new_admin_category_path %>
<div class="row">
  <div class="">
    <%= form_for categories_path, method: :get,
      class: "search_form_category" do %>
        <%= render "admin/shared/search_form" %>
    <% end %>
  </div>
  <div class="col-md-12 col-xs-12">
    <p><b><%= @categories.count %> </b><%= t ".categories" %></p>
  </div>
  <div class="col-md-12 col-xs-12">
    <div class="table-responsive">
      <table class="table table-bordered table-hover table-striped">
         <thead>
           <tr>
             <th><input type="checkbox" class="icheck-me" data-skin="square" data-color="blue"></th>
             <th><%= t ".word_count" %></th>
             <th><%= t ".name" %></th>
             <th><%= t ".description" %></th>
             <th><%= t ".created" %></th>
             <th><%= t ".action" %></th>
           </tr>
         </thead>
         <tbody>
           <% if @categories.any? %>
             <% @categories.each_with_index do |category, index| %>
               <%= render category, index: index %>
             <% end %>
           <% else %>
             <div class="alert alert-warning">
               <%= t ".not_found_categories" %>
             </div>
           <% end %>
         </tbody>
      </table>
    </div>
    <p class="text-center"><%= will_paginate @categories %></p>
  </div>
</div>
