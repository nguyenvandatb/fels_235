<%= provide :title, @word.content %>
<div class="row">
  <div class="col-lg-12">
    <% if @answers.any? %>
      <%= render @answers %>
    <% else %>
      <div class="alert alert-warning">
        <%= t ".not_found_words" %>
      </div>
    <% end %>
  </div>
</div>
